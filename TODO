- As a side-effect of loading all files in the directory if a single file
is given as argument, the initial total count is off if a single image file
is given as argument and Sxiv is open in normal mode (i.e., not thumbnail
mode). Fix that.
